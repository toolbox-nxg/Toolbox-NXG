/**
 * Domain tagger schema types. Re-exported from the canonical wiki schema so
 * module code and wiki infrastructure share a single source of truth.
 */
export type {DomainTag, DomainTagsData,} from '../../util/wiki/schemas/domaintags/schema'
